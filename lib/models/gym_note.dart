class GymNote {
  String id;
  String title;
  String content;
  DateTime dateTime;

  GymNote({
    required this.id,
    required this.title,
    required this.content,
    required this.dateTime,
  });
}